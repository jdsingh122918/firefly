{
  "name": "firefly-integration-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:headed": "HEADLESS=false vitest run",
    "test:debug": "HEADLESS=false vitest --inspect-brk"
  },
  "dependencies": {
    "msw": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "dotenv": "^16.3.1",
    "puppeteer": "^24.15.0",
    "typescript": "^5",
    "vitest": "^2.0.0"
  }
}

